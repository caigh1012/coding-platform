# 指定开发环境端口
serverPort=23572

# 可以设置 dev、perf、uat，用于调试后台接口
targetProxy=dev

# dev环境的后台接口
dev=http://127.0.0.1:8080

# perf环境的后台接口
perf=http://localhost:4000

# uat环境的后台接口
uat=http://localhost:5000

# 是否启动打包分析器 1 开启 0 关闭
isBundleAnalyzer=0